events {
    worker_connections 1024;
}

http {
    server {
        listen {{ nginx_port }} default_server;
        server_name _;

        root {{ root_dir }};
        index {{ index_file }};

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
